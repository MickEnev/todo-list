import React from 'react'
import styles from './doing.module.css'

export default function Doing() {
  return (
    <div>
        <input></input>
    <ul>
        <li>Hello</li>
        <li>Hello</li>
        <li>Hello</li>
        <li>Hello</li>
        <li>Hello</li>
    </ul>
    </div>
    
    /* TODO: Create a list of tasks that can be added to with an input field.
        - Each task should be able to be marked off as complete.
        - When a task is marked complete it gets moved to Done.
        - If a task is incomplete by the end of the day move it to failed.
        - *BONUS* If len(failed) > len(complete) by the end of the week do something bad (maybe open a zip bomb on pc?) */
  )
}
